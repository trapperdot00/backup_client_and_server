#ifndef STRING_OPERATIONS_H
#define STRING_OPERATIONS_H

#include <string>

// Remove whitespace from left, right, or both
std::string lstrip(const std::string&);
std::string rstrip(const std::string&);
std::string strip(const std::string&);

#endif
